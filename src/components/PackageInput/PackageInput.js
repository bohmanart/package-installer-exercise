import React from 'react'

const PackageInput = (props) => (
    <div>
        <h2>Input</h2>
        <pre><code>"
            {props.input}
        "</code></pre>
    </div>
)

export default PackageInput
